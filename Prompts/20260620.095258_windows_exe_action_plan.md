# User Prompt
create a new github action YML to generate a windows EXE for x64
Disable ci.yml, release.yml, stale.yml

# Goal Description
1. Create a new GitHub Actions workflow file `.github/workflows/build-windows-exe.yml` to build and upload a standalone Windows x64 executable (`webtorrent-cli-windows.exe`) using `pkg`.
2. Disable the other 3 existing GitHub Action workflows (`ci.yml`, `release.yml`, `stale.yml`) by renaming their file extensions to `.yml.disabled` so they are not parsed by GitHub Actions.

## User Review Required
We are disabling three existing workflows: `ci.yml`, `release.yml`, and `stale.yml`. This prevents them from triggering automatically.

## Proposed Changes

### GitHub Actions Workflows

#### [NEW] [build-windows-exe.yml](file:///x:/Projects_X/0_Active/C_NodeJs/webtorrent-cli-windows/.github/workflows/build-windows-exe.yml)
Create the build workflow to build the Windows EXE file.

#### [DELETE] [ci.yml](file:///x:/Projects_X/0_Active/C_NodeJs/webtorrent-cli-windows/.github/workflows/ci.yml)
#### [NEW] [ci.yml.disabled](file:///x:/Projects_X/0_Active/C_NodeJs/webtorrent-cli-windows/.github/workflows/ci.yml.disabled)
Disable by renaming.

#### [DELETE] [release.yml](file:///x:/Projects_X/0_Active/C_NodeJs/webtorrent-cli-windows/.github/workflows/release.yml)
#### [NEW] [release.yml.disabled](file:///x:/Projects_X/0_Active/C_NodeJs/webtorrent-cli-windows/.github/workflows/release.yml.disabled)
Disable by renaming.

#### [DELETE] [stale.yml](file:///x:/Projects_X/0_Active/C_NodeJs/webtorrent-cli-windows/.github/workflows/stale.yml)
#### [NEW] [stale.yml.disabled](file:///x:/Projects_X/0_Active/C_NodeJs/webtorrent-cli-windows/.github/workflows/stale.yml.disabled)
Disable by renaming.

## Verification Plan

### Automated Tests
- None.

### Manual Verification
- Verify files are renamed and no longer active in GitHub Actions workflow schema.
