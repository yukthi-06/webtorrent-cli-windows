# User Prompt
create a new github action YML to generate a windows EXE for x64
Disable ci.yml, release.yml, stale.yml

# Task list
- [x] Create the `.github/workflows/build-windows-exe.yml` file.
- [x] Verify the file content.
- [x] Disable `ci.yml` by renaming it to `ci.yml.disabled`.
- [x] Disable `release.yml` by renaming it to `release.yml.disabled`.
- [x] Disable `stale.yml` by renaming it to `stale.yml.disabled`.
